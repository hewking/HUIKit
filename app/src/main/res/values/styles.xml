<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CommTextStyle" parent="TextAppearance.AppCompat">
        <item name="android:textSize">16sp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:padding">4dp</item>
    </style>

    <declare-styleable name="AudioCircleTextView">
        <attr name="animMode" format="boolean"/>
    </declare-styleable>

    <style name="TextCompatAutoSize">
        <item name="autoSizeMaxTextSize">17sp</item>
        <item name="autoSizeMinTextSize">4sp</item>
        <item name="autoSizeStepGranularity">4sp</item>
        <item name="autoSizeTextType">uniform</item>
    </style>

</resources>
